---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "colors-demo-app.fullname" . }}
  labels:
    {{- include "colors-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "colors-demo-app.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
